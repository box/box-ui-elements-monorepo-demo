// only used for styling and structuring the elements in the page

function ElementSection({ children }) {
  return <div className="ElementSection">{children}</div>;
}

export default ElementSection;
